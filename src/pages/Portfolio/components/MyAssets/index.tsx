import { Trans } from '@lingui/react/macro'
import styled from 'styled-components'

const MyAssetsWrapper = styled.div`
  display: flex;
  flex-direction: column;
`

export default function MyAssets() {
  return <MyAssetsWrapper></MyAssetsWrapper>
}
