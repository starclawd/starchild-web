import { memo } from 'react'
import styled from 'styled-components'

const VaultDetailWrapper = styled.div`
  display: flex;
`

export default memo(function VaultDetail() {
  return <VaultDetailWrapper></VaultDetailWrapper>
})
